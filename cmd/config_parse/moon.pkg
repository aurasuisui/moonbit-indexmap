import {
  "aurasuisui/indexmap",
}

// Modern executable-package marker (replaces the deprecated `options("is-main":
// true)`), so this example builds under the `version: latest` toolchain in CI.
pkgtype(kind: "executable")
